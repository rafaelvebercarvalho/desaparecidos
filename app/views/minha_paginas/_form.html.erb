<%= form_with(model: minha_pagina, local: true) do |form| %>
  <% if minha_pagina.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(minha_pagina.errors.count, "error") %> prohibited this minha_pagina from being saved:</h2>

      <ul>
      <% minha_pagina.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit :Gravar, class: 'btn btn-success' %>
  </div>
<% end %>
